# AppTareasV1
Table tipo Kanban para gestión de tareas


Comando utiles
ejecutar la app:
    cliente: ng serve -o
    API: dotnet watch run

build:
    cliente: ng build --configuration production --base-href /AppTareasV1/
    API: dotnet publish -c Release -o ./publish