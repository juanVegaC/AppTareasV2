export const environment = {
    // Production flags for Angular build served by IIS
    production: true,
    // When the Angular app is hosted by the ASP.NET Core site in IIS,
    // use a relative API path so it works on any host/virtual directory.
    apiUrl: 'api/',
    idleTimeSeconds: 300,
    idleTimeSecPOPUP: 60
  };
  